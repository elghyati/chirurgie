{% extends 'admin/base.html.twig' %}

{% block title %}Hello OrderStatusController!{% endblock %}

{% block body %}

    <div class="content">
        <div class="container-fluid">
            <div class="row">
                <div class="col-sm-12">
                    <div class="page-title-box">
                        <h4 class="page-title">Gestion des statuts</h4>
                        <ol class="breadcrumb">
                            <li class="breadcrumb-item"><a href="{{ path('admin_dashboard_index') }}">ChirurgieMed</a></li>
                            <li class="breadcrumb-item active">Statuts</li>
                        </ol>
                    </div>
                </div>
            </div>
            <!-- end row -->

            <div class="page-content-wrapper">
                <div class="row">
                    <div class="col-12">
                        <div class="card m-b-20">
                            <div class="card-body">



                                <h4 class="mt-0 header-title">Liste statuts des commandes</h4>
                                <table id="datatable-buttons" class="table table-striped dt-responsive table-hover nowrap"  style="border-collapse: collapse; border-spacing: 0; width: 100%;">

                                    <thead class="table-dark">
                                    <th>ID</th>
                                    <th>Libelle</th>
                                    <th></th>
                                    </thead>
                                    <tbody>

                                        {% for status in orderstatus %}
                                            <tr border-bottom>
                                                <td> {{ status.id }}</td>
                                                <td> {{ status.libstatus }}</td>



                                                <td class="ali">
                                                    <div class="form-inline justify-content-center">
                                                        {#                                                        <a href="{{ path('admin_order_status_show', {'id': OrderStatus.id}) }}" class="btn btn-primary mr-2" data-toggle="tooltip" data-placement="top" title data-original-title="Détails" ><i class="fas fa-eye"></i>  </a>#}
                                                        <a href="{{ path('admin_order_status_edit', {'id': status.id}) }}" class="btn btn-primary mr-2" data-toggle="tooltip" data-placement="top" title data-original-title="Modifier"><i class="fas fa-pencil-alt"></i>  </a>

{#                                                        <a class="btn btn-danger" href="{{ path('admin_order_status_delete', {id: status.id}) }}">delete</a>#}
                                                    </div> 
                                                </td>


                                            </tr>
                                        {% endfor %}
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div> <!-- end col -->
                </div> <!-- end row -->
            </div>
            <!-- end page content-->
            <div class="form-inline align-items-lg-center my-3">
                <a class="btn btn-success mr-3" href="{{ path('admin_order_status_new') }}"><i class="fas fa-plus"></i> Ajouter un statut</a>


            </div> <!-- container-fluid -->
        </div> <!-- content -->
    {% endblock %}
